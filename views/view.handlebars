<div class="job-view-container" id="container">
    <div class="row text-center">
        <a href="/" id="back-link maincolor">Voltar</a>
        <div id="img-container" class="col-md-12">
            <img src="/img/company.svg" alt="Company">
        </div>
        <div class="col-md-12">
            <h1 class="main-color">{{job.dataValues.titulo}}</h1>
            <p class="bold">Quem eles estão buscando:</p>
            <p class="job-desc">{{job.dataValues.descricao}}</p>
            <p><span class="bold">O salário informado é:</span> R${{job.dataValues.salario}}</p>
            <p>
                Para trabalhar na <span class="bold main-color">{{job.dataValues.empresa}}</span> envie um email para a <a href="mail:to{{job.dataValues.email}}">{{job.dataValues.email}}</a> 
            </p>
        </div>
    </div>
</div>